---
title: Input
description: Surface for accepting user input
---

```tsx
import { Input, SearchSVG } from '@ensdomains/thorin'
```

```tsx live=true expand=true
<Input
  icon={<MagnifyingGlassSimpleSVG />}
  label="Wallet Address"
  placeholder="0xA0Cf…251e"
  prefix="hello"
  suffix="world"
/>
```

## Props

<PropsTable sourceLink={sourceLink} types={types} />

## Description

```tsx live=true
<Input
  description="The Ethereum address where raised funds will be sent from the crowdfund."
  label="Wallet Address"
  placeholder="0xA0Cf…251e"
/>
```

## Error

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input
    description="Default input"
    error="Error message"
    label="Default"
    placeholder="Placeholder"
    value="Invalid input"
  />
  <Input
    description="Input with icon"
    error="Error message"
    icon={<MagnifyingGlassSimpleSVG />}
    label="Icon"
    placeholder="Placeholder"
    showDot
    value="Invalid input"
  />
  <Input
    description="Input with prefix"
    error="Error message"
    icon={<MagnifyingGlassSimpleSVG />}
    label="Prefix"
    placeholder="Placeholder"
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    showDot
    value="Invalid input"
  />
  <Input
    description="Input with suffix"
    error="Error message"
    icon={<MagnifyingGlassSimpleSVG />}
    label="Suffix"
    placeholder="Placeholder"
    showDot
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
    value="Invalid input"
  />
  <Input
    description="Input with prefix and suffix"
    error="Error message"
    icon={<MagnifyingGlassSimpleSVG />}
    label="Multiple"
    placeholder="Placeholder"
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    showDot
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
    value="Invalid input"
  />
</Box>
```

## Affixes

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input
    description="Prefix using a svg component."
    icon={<MagnifyingGlassSimpleSVG />}
    label="Search"
    placeholder="Name or wallet address"
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
  />
</Box>
```

## Show Dot

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input
    icon={<MagnifyingGlassSimpleSVG />}
    label="Placeholder"
    placeholder="Enter text here..."
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    showDot
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
  />
  <Input
    icon={<MagnifyingGlassSimpleSVG />}
    label="Validated"
    placeholder="Enter text here..."
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    showDot
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
    validated
  />
  <Input
    error="Error messsage"
    icon={<MagnifyingGlassSimpleSVG />}
    label="Error"
    placeholder="Enter text here..."
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    showDot
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
  />
</Box>
```

## Disabled

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input
    description="Prefix using a svg component."
    disabled
    icon={<MagnifyingGlassSimpleSVG />}
    label="Search"
    placeholder="Name or wallet address"
    prefix={
      <>
        <MagnifyingGlassSVG />
        Prefix
      </>
    }
    suffix={
      <>
        Suffix
        <EyeSVG />
      </>
    }
  />
</Box>
```

## Clearable

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input
    clearable
    label="Clearable input"
    placeholder="Foo bar baz"
    type="text"
  />
  <Input
    clearable={false}
    label="Not clearable input"
    placeholder="Foo bar baz"
  />
</Box>
```

## Sizes

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input label="Small" placeholder="Placeholder" size="small" />
  <Input label="Medium" placeholder="Placeholder" size="medium" />
  <Input label="Large" placeholder="Placeholder" size="large" />
  <Input label="Extra Large" placeholder="Placeholder" size="extraLarge" />
</Box>
```

## Hide Label

```tsx live=true
<Input hideLabel label="Wallet Address" placeholder="Wallet Address" />
```

## onClickAction

```tsx live=true
<Input
  actionIcon={getState('show-password') ? <EyeSVG /> : <EyeStrikethroughSVG />}
  type={getState('show-password') ? 'text' : 'password'}
  value="password"
  onClickAction={() => toggleState('show-password')}
/>
```

## alwaysShowAction

```tsx live=true
<Input alwaysShowAction clearable />
```

## Read Only

```tsx live=true
<Input readOnly value="Read only input" />
```

## Icon Width

```tsx live=true
<Box display="flex" flexDirection="column" gap="$4">
  <Input
    icon={<div style={{ width: '32px', height: '32px', background: 'red' }} />}
    iconWidth="8"
    label="Custom icon width"
  />
  <Input icon={<FlameSVG />} iconWidth="8" label="Custom icon width" />
</Box>
```
