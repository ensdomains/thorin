import * as React from 'react'

import { Snippet } from '../../../types'
import { Typography } from './Typography'

export const snippets: Snippet[] = [
  {
    name: 'Basic',
    code: <Typography>Typography</Typography>,
  },
]
